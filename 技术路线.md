# 技术路线

面向二次开发者。普通使用见 `README.md`，服务器运维手册见 `docs/OPERATIONS.md`，部署工件与参数全表见 `deploy/README.md`，过程记录与全部决策档案见 `PLAN.md`（§12 决策、§13 进度含两次生产事故复盘）。

## 1. 总体架构

```
Windows 本地开发（uv 原生，不装 Docker）
        │  git push v* 标签
        ▼
GitHub Actions（离线测试 → buildx 构建 → 推 GHCR + Release 部署包）
        │  服务器只拉取
        ▼
Linux 服务器（Docker Compose 三容器 + systemd 定时器 + 宿主 Nginx/certbot）
```

设计动机：**缓解服务器压力**。服务器不保存 Git 仓库与源码，只承担每日一次的 worker 运行和静态托管；代码只存在于 GHCR 预构建镜像内。服务器侧仅保留：compose 清单、`config/` 配置子目录、systemd 单元、Nginx 配置、持久化卷。

技术栈：Python 3.13 + uv；依赖仅 6 个——jinja2（渲染）、httpx（抓取）、feedparser（RSS）、trafilatura（正文提取）、nh3（HTML 清洗）、tzdata。存储用标准库 SQLite，面板用标准库 `http.server`，无 Web 框架。质量工具：ruff（E,F,W,I,UP,B，行宽 100）、pytest（离线测试为主，网络测试标记 `network` 默认跳过）。

## 2. 代码地图

```
src/news_digest/
├── cli.py              命令入口：fetch / translate / build / run / preview /
│                       preview-email / send-email / admin / import-edition
├── config.py           环境配置（NEWS_* / TRANSLATION_* / SMTP_*），.env.local 由 CLI 加载
├── models.py           数据模型（Article / Edition / BriefItem …）
├── pipeline.py         流水线编排：抓取入库→选题→翻译→构建 的粘合层
├── sources/            registry（7 个来源定义）、feeds（RSS 解析）、http（加固客户端）
├── extractors/body.py  trafilatura 正文提取 + nh3 白名单清洗
├── selection/          score(选题打分) + dedupe(标题相似度去重)
├── translation/        client(SSE 流式 OpenAI 兼容) + schema(严格校验/PROMPT_VERSION) + service(缓存/编排)
├── storage/db.py       SQLite（schema v1：meta / articles / briefs）
├── rendering/          pages(Jinja2 站点模板) + email(邮件 HTML)
├── delivery/           publisher(发布/切换/修剪) + mailer(SMTP)
├── preview_server.py   本地预览 + 生产管理面板（同一实现，serve_static 开关区分）
└── textutil.py         文本工具
```

## 3. 数据流水线

每日 `run` = fetch → 入库 → select → translate → build，各环节也可单独执行。

**抓取（fetch）**：7 个来源（BBC、卫报、半岛、France 24、DW、NPR 为全文源；NYT 仅进简讯）。RSS 拿列表 → 逐篇抓页面 → trafilatura 提取正文 → nh3 清洗。HTTP 客户端安全加固：域名白名单、私网 IP 阻断（代理生效时豁免本地 DNS 校验——用户为 fake-ip 代理模式，198.18.0.0/15 会误触私网判定）、重定向次数上限、响应 3 MB 上限。抓取窗口默认 24 小时。

**入库（storage）**：抓到即 upsert 进 SQLite。关键约束：**未译新行绝不覆盖已译旧行的译文**——同一篇文章反复抓取时译文保留。`import-edition` 子命令可导入历史期 JSON（创刊号即由此进服务器归档）。

**选题（selection）**：从当日文章中选 6 篇主文。打分 = 时新性 + 全文可用加成 + 长度加成；约束：单来源最多 2 篇；标题相似度 ≥ 0.85 视为同题互斥。落选但已有译文的自动降级为双语简讯（翻译投入跨选题保留）。

**翻译（translation）**：只译 6 篇主文（成本控制）。OpenAI 兼容接口，**SSE 流式**——非流式长文会撞中转 Nginx 读超时出 504。必传 `max_tokens`（Claude 系后端强制要求）；不传 `temperature`（推理模型已弃用该参数）。输出走 `schema.py` 严格校验（JSON 结构 + 段落数对齐），不合格整篇拒绝，宁缺毋滥，可 `--redo` 重试。缓存键 = `sha256(原文):模型名:PROMPT_VERSION`（当前 p2，官方文风），命中零成本，支持断点续跑；纯缓存命中时无 API 也能工作（创刊号译文复水即用此路径）。

**构建（build + publisher）**：Jinja2 渲染全站静态页 → 写入 `releases/<日期-序号>` → 原子切换 `current` 链接（POSIX symlink，Windows 本地为 junction）→ 修剪保留最近 5 版。两条从生产事故换来的规则（详见 PLAN §13 2026-07-27）：
- **序号 = 当日已有最大值 + 1，绝不复用空缺**。曾因复用被修剪掉的 `-01`，新版本按名称排序恰为"最旧"，发布后立即被修剪器删除，current 悬空 404；
- **修剪器无论排序结果如何，绝不删除刚发布的版本**（`keep_name` 双保险）。

**邮件（delivery.mailer，可选）**：SMTP SSL 发送当日双语摘要，`--smoke` 无链接测试模式。

## 4. 服务器形态：三容器

| 容器 | 镜像 | 职责 | 安全基线 |
|---|---|---|---|
| worker | news-digest-worker | 每日一次性运行完整流水线，跑完即退 | read_only 根、cap_drop ALL、256 MB 上限 |
| web | news-digest-web | Nginx 托管静态站，绑 `127.0.0.1:8618`（容器内 8080），/healthz 健康检查 | read_only、cap_drop ALL、32 MB 上限 |
| admin | worker 同镜像 | 管理面板常驻，代码硬绑回环 → host 网络实现"只监听宿主 127.0.0.1:8619" | read_only、cap_drop ALL、32 MB 上限；唯一宿主挂载 = `config/` 子目录 |

数据经两个命名卷（`/data` SQLite、`/site` 站点）持久化；worker 镜像内固定 `NEWS_DATA_DIR` 等路径 ENV，**部署 `.env` 中不得覆盖**（会写只读根失败）。定时：systemd timer `OnCalendar=*-*-* 08:00:00 Asia/Shanghai`。宿主 Nginx 反代 8618/8619 + certbot 自动续期。

## 5. 部署链

`release.yml`（推 `v*` 标签触发）：校验 `__version__` 与标签一致 → 复跑离线测试 → buildx 构建双镜像推 GHCR → 输出 digest → 打包 `news-digest-deploy.tgz` 附到 Release。

服务器侧一键：`install.sh`（远程模式自动下载最新 Release 部署包）→ `preflight.sh` 只读体检 → `bootstrap.sh` 幂等部署（镜像、容器、定时器、Nginx、HTTPS、首次生成面板口令）。全链 `ND_*` 环境变量参数化（OWNER/VERSION/APP_DIR/DOMAIN/WEB_PORT/ADMIN_PORT/CERTBOT_EMAIL），换域名换端口不改脚本。

本地一键：`deploy.bat` → `deploy-all.ps1`（ssh-agent 免重复口令、推分支后**单独推版本标签**、等待 CI、下载同一 Release 的镜像 digest、GHCR 只读登录、server-push、smoke check）。部署链不读取 `.env.local`，也不传输 API/SMTP 运行密钥；首次安装生成关闭状态的服务器配置，之后由 Admin 写入运行配置。

### Windows 维护者发布与远程部署

`deploy.bat` 面向拥有仓库发布权限的维护者，不是普通用户安装入口。运行前必须满足：工作树干净且当前分支为 `main`；`src/news_digest/__init__.py` 的版本已有对应 annotated tag，tag 指向当前 `HEAD`；GitHub CLI 已登录；Windows OpenSSH Client 可用；SSH Key 能以 root 登录目标 Linux；准备只有 `read:packages` 权限的 GHCR PAT。

目标参数不会自动发现，必须由维护者提供：`Server`、`KeyPath`、`Owner`、`AppDir`、`Domain`、`CertbotEmail`，以及可选的 `WebPort`、`AdminPort`。首次运行使用交互模式，让 ssh-agent、Key passphrase 和 PAT 通过受控提示完成：

```powershell
.\deploy.bat -Interactive -Server root@server.example.com -KeyPath C:\keys\deploy_ed25519 -Owner yi-lings -AppDir /opt/news-digest -Domain news.example.com -CertbotEmail ops@example.com
```

无人值守时可改用 `ND_SERVER`、`ND_KEY_PATH`、`ND_OWNER`、`ND_APP_DIR`、`ND_DOMAIN`、`ND_CERTBOT_EMAIL`、`ND_WEB_PORT`、`ND_ADMIN_PORT` 环境变量，GHCR PAT 使用 `ND_GHCR_TOKEN`。个人生产值应保存在仓库外的受限 wrapper 中；本地部署日志写入 `var/deploy-log.txt`。

## 6. 管理面板与安全模型

面板 = `preview_server.py` 生产模式（`serve_static=False`，静态回落关闭——admin 容器绝不可能吐出 `/config` 内容）。认证：品牌登录页 + HMAC 签名会话 Cookie（7 天，Secure/HttpOnly/SameSite=Strict）；口令哈希 apr1（纯标准库实现，对拍过 openssl 向量）；登录失败恒定延迟防爆破防时序；改口令必须验当前口令，成功后轮换 session-secret 使所有会话下线。密钥流动路径：只存在于服务器 `config/`（root:600，目录 750）与用户本地 `.env.local`（gitignored），**永不进 Git、CI、镜像、日志、聊天**；面板读写档案时响应永远掩码。Nginx 层：`/admin/` 独立 location CSP（放行面板内联脚本，作用域不外溢）；站点与面板均 `noindex`。

## 7. 版本与发布纪律

- **标签一经推送绝不移动、绝不 force-push**；任何变更递增新标签（rc4 标签分叉事故的教训：本地重打标签 ≠ 远端，服务器跑旧构建而外部探测误判）。
- 生产固定镜像 **digest** 而非 tag（tag 理论可覆盖，digest 不可变；回滚 = 改回上一条 digest 或切 current）。
- `__version__` 与标签不一致 CI 直接失败。
- 一次 push 超过 3 个标签 GitHub 会抑制 push 事件（CI 不触发）——版本标签必须单独推。

## 8. 快速开始与本地开发

本节面向源码开发与离线演示；普通用户部署服务器请按 `README.md` 的“快速部署”操作。

环境要求：Python 3.13、uv 和 Git。本地开发支持 Windows、Linux 和 macOS，不要求 Docker。

```bash
git clone https://github.com/Yi-Lings/news-digest.git
cd news-digest
uv sync --locked
```

先使用 fixture 构建完整演示，不访问新闻源、provider 或 SMTP：

```bash
uv run news-digest build --fixtures tests/fixtures/demo
uv run news-digest preview --port 8618 --automation-demo
```

打开站点 `http://127.0.0.1:8618/` 和 Admin `http://127.0.0.1:8618/admin/`。`--automation-demo` 使用独立 SQLite、固定任务和 fake provider，不读取正式任务或发送邮件。

需要连接真实新闻源和 provider 时，先创建本地配置。Windows PowerShell：

```powershell
Copy-Item .env.example .env.local
```

Linux/macOS：

```bash
cp .env.example .env.local
```

至少确认：

```dotenv
NEWS_SITE_URL=http://127.0.0.1:8618
NEWS_TIMEZONE=Asia/Shanghai
NEWS_DATABASE_PATH=var/data/news.db
NEWS_OUTPUT_PATH=var/site
EMAIL_DELIVERY_ENABLED=false
PUBLIC_SUBSCRIPTION_ENABLED=false
```

启动 preview 后，在 Admin 新增 provider，完成连接测试和正式 schema 测试，启用并设为唯一默认。先预览执行计划，再显式允许真实调用：

```bash
uv run news-digest run
uv run news-digest run --yes
```

CLI 速查：

| 命令 | 用途 |
|---|---|
| `news-digest fetch` | 抓取、规范化并保存候选新闻 |
| `news-digest translate` | 显示当日翻译计划；加 `--yes` 才真实调用 provider |
| `news-digest build` | 从 SQLite 生成静态站点和 release manifest |
| `news-digest run` | 执行抓取、选题、翻译、增量构建和受控投递 |
| `news-digest preview` | 在 `127.0.0.1` 启动本地站点与 Admin |
| `news-digest preview-email` | 离线生成邮件预览到 `var/mail/` |
| `news-digest send-email` | 显式发送已生成刊物，需要 `--yes` 和投递门禁 |
| `news-digest import-edition` | 幂等导入一期 JSON，保留已有翻译成果 |
| `news-digest admin` | 启动生产 Admin 回环服务，交给 HTTPS 反向代理 |

查看完整参数：

```bash
uv run news-digest --help
uv run news-digest run --help
```

常用开发命令：

```text
uv run ruff check . && uv run pytest   # 离线质量检查
preview.bat                            # Windows 构建并预览 127.0.0.1:8618
daily.bat                              # Windows 本地执行一次完整流水线
deploy.bat                             # 维护者发布并远程部署
```

Windows 专项：源码 LF（`.gitattributes`，仅 `*.bat` CRLF）、`PYTHONUTF8=1`、current 用 NTFS junction、PowerShell 5.1 脚本保持 ASCII。

## 9. 已知 backlog（不阻塞）

配置文件写入改原子写 + 锁；面板内联脚本抽出为静态 `/admin/app.js` 以移除 `unsafe-inline`；两处 CSP 增加 `frame-ancestors 'none'`（server 层与 `/admin/` location 需同加——将来开 HSTS 也是同一个双处陷阱）；preflight IPv4/IPv6 出口对比误报；面板应用层审计日志（现由宿主 Nginx access log 兜底）。
